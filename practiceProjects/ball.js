// I'm gonna make this function work such that it can switch between the default color of the page and the new color the user inputs. A work in progress, though. Alternate ideas may be arriving soon.
function changeBackground(color) {
   var oldColor = document.body.style.backgroundColor;
   document.body.style.backgroundColor = color;
}